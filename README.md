# HYDRA v2.3.1

[![Version](https://img.shields.io/badge/version-2.3.1-blue.svg)]()
[![Python](https://img.shields.io/badge/python-3.10+-green.svg)]()
[![License](https://img.shields.io/badge/license-MIT-blue.svg)]()
[![Platform](https://img.shields.io/badge/platform-Ubuntu%20%7C%20Debian-lightgrey.svg)]()

> HYDRA – платформа для развёртывания прокси-серверов на базе **Sing-Box** как единого оркестратора трафика. Модульная архитектура: 17 плагинов (9 транспортов, 3 надстройки, 4 безопасности) с единой политикой роутинга, DNS и безопасности.

> [!IMPORTANT]
> На данный момент полностью готовы, отлажены и стабильно работают плагины **AmneziaWG 2.0** (интегрирован напрямую в ядро Sing-Box через TPROXY), **Mieru**, **AnyTLS**, **TrustTunnel**, **NaiveProxy** (работает через HTTP/2), **MTProto (Telemt)** (Rust MTProxy с защитой от DPI и продвинутой TUI-статистикой) и **qWDTT** (WireGuard over TURN с изолированным парольным управлением; *внимание: интеграция с Telegram-ботом находится в разработке и еще не готова*). Полностью готов и отлажен **подписочный функционал** для генерации клиентских подписок.
> В разделе безопасности полностью готовы и снабжены интерактивными пультами управления плагины **Fail2ban** (защита от перебора sing-box/sshd), **Honeypot** (ловушка сканеров с авто-баном) и **IPBan** (ручная блокировка IP/CIDR/диапазонов/ASN).
> В разделе сетевых служб полностью готовы модули **WARP** с выборочной маршрутизацией и **DNSCrypt** для системного шифрования DNS (с автоматическим замером latency и выбором серверов).
> Все остальные плагины (транспорты, надстройки) находятся на этапе активной разработки (WIP).

---

## Архитектура

```
Клиент → [Транспортный демон] → nftables TPROXY → Sing-Box (inbound)
                                                     │
                          ┌──────────────────────────┤
                          ↓                          ↓
                    WARP (outbound)             Direct (outbound)
                          │                          │
                    Cloudflare                  Интернет
                          │
                    ┌─────┴──────┬─────────────────────────┐
                    ↓            ↓                         ↓
              DNSCrypt:5300  Fail2ban                  IPBan/Honeypot
              (DNS шифрование) (бан по логам)            (ручной/авто бан)
```

Каждый протокол — плагин, отдающий фрагмент конфига для Sing-Box. Конфиг собирается динамически: orchestrator → collect_fragments → generate_config → nft.apply_tproxy → write_config → reload.

---

## Стек протоколов

### Транспорты (TRANSPORT)
| Протокол | Плагин | Особенности | Статус |
|---|---|---|---|
| AmneziaWG 2.0 | `amneziawg` | Kernel-модуль, интегрирован в ядро Sing-Box через TPROXY | 🟢 Готов (Ready) |
| Mieru | `mieru` | mTLS + random padding | 🟢 Готов (Ready) |
| NaiveProxy | `naive` | Caddy (TLS) + fake-site (работает через HTTP/2) | 🟢 Готов (Ready) |
| MTProto | `telemt` | Telegram MTProto, multi-user | 🟢 Готов (Ready) |
| qWDTT | `wdtt` | WG over TURN с изолированным парольным управлением (интеграция Telegram-ботов в разработке) | 🟢 Готов (Ready) |
| AnyTLS | `anytls` | TLS-shaped tunnel с padding scheme | 🟢 Готов (Ready) |
| TrustTunnel | `trusttunnel` | Защищённый туннель для обхода блокировок | 🟢 Готов (Ready) |
| ShadowTLS | `shadowtls` | TLS-обертка с имитацией рукопожатия доверенных сайтов | 🟡 В планах (Roadmap) |

### Сетевые службы (ENHANCEMENT)
| Плагин | Что делает | Статус |
|---|---|---|
| DNSCrypt | Системный DNS-прокси :5300 (DoH/DNSCrypt) | 🟢 Готов (Ready) |
| WARP | Cloudflare WARP — выборочный роутинг через WireGuard | 🟢 Готов (Ready) |

### Безопасность (SECURITY)
| Плагин | Что делает | Статус |
|---|---|---|
| Fail2ban | Защита от перебора (sing-box/sshd) | 🟢 Готов (Ready) |
| Honeypot | Ловушка для сканеров с авто-баном | 🟢 Готов (Ready) |
| IPBan | Ручная блокировка IP/CIDR/диапазона/ASN | 🟢 Готов (Ready) |

---

## Описание Ключевых Функций

### Cloudflare WARP (Выборочный роутинг)

Модуль интеграции с Cloudflare WARP (`warp`) предназначен для выборочного перенаправления трафика. Он никогда не перехватывает весь трафик по умолчанию, позволяя пустить через сеть Cloudflare только выбранные ресурсы.

**Возможности модуля:**
* **Локальные правила**: Добавление и удаление собственных доменов, IP-адресов и CIDR-подсетей через интерактивное меню TUI.
* **Встроенные списки правил (itdoginfo)**:
  * **РФ-сервисы** (`outside-raw.lst`): Российские ресурсы (используются для скрытия IP VPS за серверами WARP, чтобы РФ зонды не обнаруживали его).
  * **GEO-block** (`geoblock.lst`): Популярные зарубежные ресурсы, заблокированные на территории РФ.
  * **GoogleAI** (`google_ai.lst`): Сервисы ИИ от Google (Gemini, Google AI Studio и др.).
* **Автоматическое обновление**: Фоновый агент `sync_agent` раз в 24 часа автоматически скачивает включенные списки, кэширует их и обновляет конфигурацию маршрутизации Sing-Box.
* **Современный стек**: Полная интеграция с Sing-Box 1.13.0+ через нативную секцию `endpoints` (без лишних `outbounds` и прослоек).

### HAProxy Роутер (TLS SNI Мультиплексор)

При активации двух или более TLS-транспортов (например, AnyTLS, NaiveProxy, TrustTunnel), слушающих порт 443, HYDRA автоматически разворачивает и настраивает **HAProxy** как единую точку входа.

**Схема работы:**
```
Входящий TLS-трафик (Порт 443)
              │
              ▼
       ┌─────────────┐
       │   HAProxy   │ (Чтение SNI из TLS Client Hello без расшифрования)
       └──────┬──────┘
              │
     ┌────────┼────────┬─────────────┐
     ▼        ▼        ▼             ▼
  [SNI-1]  [SNI-2]  [SNI-3]     [Неизвестный/Default]
     │        │        │             │
     ▼        ▼        ▼             ▼
 NaiveProxy AnyTLS TrustTunnel  Сайт-заглушка (Маскировка)
  (:10443)  (:10444) (:10445)   (Защита от активного сканирования)
```

**Как это работает:**
* **TCP SNI Мультиплексирование**: HAProxy слушает порт 443 и при входящем соединении считывает TLS Client Hello.
* **Маршрутизация по домену**: На основе SNI (Server Name Indication) запрос без расшифрования (что гарантирует абсолютную безопасность и скрытность) перенаправляется на нужный внутренний порт соответствующего плагина:
  * NaiveProxy → порт `10443`
  * AnyTLS → порт `10444`
  * TrustTunnel → порт `10445`
* **Маскировка (Decoy)**: Запросы с неизвестными доменами сбрасываются или перенаправляются на сайт-заглушку, защищая прокси от обнаружения активным сканированием.

---

## Тестирование и Диагностика VPS

В менеджер встроен полноценный интерактивный пульт отладки и мониторинга работы VPS (`Опция 8` в главном меню). Он позволяет выявлять проблемы со скоростью, маршрутизацией, гео-блокировками и производительностью системы. Все тесты выполняются неинтерактивно с отображением результатов в TUI-рамках.

| Инструмент | Что делает | Какую информацию дает |
|---|---|---|
| 🌍 Геолокация и провайдер (GeoIP) | Опрос внешних баз данных GeoIP | Определение GeoIP для проверки смены региона. |
| 🛡️ Доступность ресурсов (Censorcheck) | Тест доступности популярных зарубежных сайтов | Проверка блокировок сервисов (OpenAI, Netflix, YouTube) напрямую с сервера. |
| 🛡️ Обход DPI-фильтров (Censorcheck DPI) | Анализ прохождения трафика через ТСПУ РФ | Проверка фильтрации и замедления трафика российскими провайдерами. |
| ⚡ Тест скорости до РФ (iPerf3) | Многопоточный замер через iPerf3 до узлов в РФ | Измерение пропускной способности (Download/Upload) и задержки (Ping) до рунета. |
| 🌐 Тест скорости: Мир (Global) | HTTP-загрузка файлов с 14 мировых дата-центров | Замер скорости скачивания и Ping до серверов США, Европы, Азии и Австралии. |
| 💻 Производительность CPU (Sysbench) | Стресс-тест процессора в один поток | Оценка вычислительной мощности CPU (событий в секунду, лимиты, задержки). |

---

## Установка

### Требования

| Параметр | Значение |
|---|---|
| ОС | Ubuntu 20.04+, Debian 11+ |
| Python | 3.10+ |
| RAM | от 512 МБ |
| Диск | от 2 ГБ |
| Права | root |
| Сеть | публичный IP, домен (для NaiveProxy) |

### Автоматическая (рекомендуется)

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/gr33nimax/HYDRA-ULTIMATE/main/bootstrap.sh)
```

Скрипт сам установит Python, Sing-Box, клонирует HYDRA в `/opt/hydra` и запустит TUI.

### Ручная

```bash
git clone https://github.com/gr33nimax/HYDRA-ULTIMATE /opt/hydra
cd /opt/hydra
sudo python3 main.py
```

### После установки

В TUI:

1. **Протоколы** → включить транспорты (AmneziaWG, Mieru, Naive и т.д.)
2. **Сетевые службы** → DNSCrypt, WARP
3. **Безопасность** → Fail2ban, Honeypot, IPBan
4. **Пользователи** → Добавить → готовы ссылки и QR

Проверить статус:

```bash
sudo hydra status
journalctl -u sing-box -n 20 --no-pager
```

---

## Структура проекта

```
hydra/
├── main.py                     # Точка входа
├── bootstrap.sh                # Установщик
├── hydra/
│   ├── core/
│   │   ├── state.py            # Типизированное состояние + миграции
│   │   ├── singbox.py          # Генератор конфигов Sing-Box
│   │   ├── orchestrator.py     # Единый pipeline apply_config
│   │   ├── nft.py              # nftables TPROXY
│   │   └── systemd.py          # systemd-хелперы
│   ├── plugins/
│   │   ├── base.py             # Абстрактный контракт v2
│   │   ├── registry.py         # Реестр + discovery + collect_fragments
│   │   ├── amneziawg/          # AmneziaWG 2.0
│   │   ├── mieru/              # Mieru (reference impl)
│   │   ├── naive/              # NaiveProxy
│   │   ├── telemt/             # MTProto
│   │   ├── wdtt/               # qWDTT
│   │   ├── dnscrypt/           # DNSCrypt-proxy
│   │   ├── warp/               # Cloudflare WARP
│   │   ├── fail2ban/           # Fail2ban
│   │   ├── honeypot/           # Honeypot
│   │   └── ipban/              # IPBan
│   ├── services/
│   │   ├── subscriptions/      # Генератор подписок
│   │   ├── telegram/           # Telegram-боты (в разработке)
│   │   ├── traffic.py          # Агрегация трафика
│   │   └── sync_agent.py       # Лимиты/TTL
│   ├── utils/
│   │   ├── firewall.py         # UFW/iptables helper
│   │   ├── downloader.py       # GitHub releases
│   │   ├── crypto.py           # Ключи/пароли
│   │   └── net.py              # IP/arch
│   └── ui/
│   │   ├── tui.py              # TUI-фреймворк
│   │   └── menus.py            # Меню
└── tests/                      # 230+ тестов
```

---

## История изменений (Changelog) и этапы разработки

Проект развивался поступательно: от закладки фундамента архитектуры v2.0 до интеграции сложных сетевых протоколов и систем учета. Ниже описана детальная хронология реализации компонентов.

### Сводная таблица релизов

| Дата | Логическая версия | Тип | Ключевой фокус этапа |
| :--- | :--- | :--- | :--- |
| **05.07.2026** | **v2.3.1 (Текущая)** | `fix` | Стабилизация оркестратора, фиксы подписок и первого запуска. |
| **04.07.2026** | **v2.3.0-beta** | `feat` | HTTPS-сервер подписок, интеграция MTProxy (Telemt) и qWDTT, VPS-диагностика. |
| **03.07.2026** | **v0.5 BETA** / *v2.2.0* | `feat` | Демон учета трафика (Traffic Daemon), Fail2ban/IPBan/Honeypot, AnyTLS, TrustTunnel, WARP. |
| **02.07.2026** | **v2.1.1** | `fix` | Отладка авторизации NaiveProxy и Caddy. |
| **01.07.2026** | **v0.0.3 BETA** / *v2.1.0* | `feat` | Переход на `sing-box-extended`, нативный Mieru inbound, плагин NaiveProxy, TPROXY nftables. |
| **30.06.2026** | **v2.0.1-beta** | `feat` | Авто-выбор подсетей AmneziaWG, лимиты MTU, геометрия и рамки TUI. |
| **29.06.2026** | **v2.0.0** | `release` | Старт HYDRA v2.0: новое архитектурное ядро, Orchestrator Pipeline, чистка legacy. |

---

### Хроника поэтапного построения системы

<details open>
<summary><b>Развернуть детальное описание этапов разработки</b></summary>

#### Этап 1: Архитектурное ядро и Orchestrator (29 июня 2026 г. — v2.0.0)
* **Цель**: Полный отказ от старого монолитного установщика xray/vless и переход к модульной системе плагинов.
* **Что сделано**:
  * Удален весь legacy-код.
  * Разработан класс `State` (`state.py`), реализующий строго типизированную схему базы состояний **State Schema v2** (хранение прав, портов, пользователей и настроек TPROXY).
  * Реализован конвейер применения конфигураций `orchestrator.py` (**Orchestrator Pipeline**): динамический сбор фрагментов конфигов от всех активных плагинов (`collect_fragments`), их объединение, проверка на ошибки валидации Sing-Box, обновление правил брандмауэра и мягкий перезапуск служб.
  * Переписан интерфейс терминала TUI (`menus.py`, `tui.py`), заложен новый TUI-фреймворк.

#### Этап 2: Интеграция Sing-Box и AmneziaWG 2.0 (30 июня 2026 г. — v2.0.1-beta)
* **Цель**: Настройка первого stealth-транспорта на базе WireGuard с защитой от DPI.
* **Что сделано**:
  * Реализована TPROXY-маршрутизация для AmneziaWG.
  * Настроен **автоматический динамический выбор бесконфликтной подсети**: плагин сканирует локальные интерфейсы сервера и выбирает подсеть (например, `10.67.67.0/24`), которая гарантированно не пересекается с существующими сетями.
  * Задано жесткое ограничение MTU до `1376` на клиентах для предотвращения дропов пакетов (PMTUD) на виртуальных серверах.
  * Внесены исправления в расчет геометрии TUI рамок: сложные Emoji и CJK символы теперь считаются как занимающие 2 ячейки в терминале, что предотвратило сползание интерфейса.

#### Этап 3: Переход на sing-box-extended и транспорт Mieru (1 июля 2026 г. — v2.1.0-beta / v0.0.3 BETA)
* **Цель**: Переход на расширенное ядро Sing-Box и запуск mTLS-протокола Mieru.
* **Что сделано**:
  * Скрипты переписаны на установку **sing-box-extended** (extended-версия с поддержкой дополнительных inbounds).
  * Плагин Mieru полностью переписан: вместо отдельного Go-процесса он интегрирован как нативный inbound внутри ядра Sing-Box.
  * Внедрены функции `ss`-парсинга для мониторинга активных IP-клиентов Mieru.
  * Настроена вставка правил учета трафика iptables на позицию 1, чтобы гарантировать подсчет байт в обход общих разрешающих правил.
  * В nftables исправлен цикл маршрутизации TPROXY добавлением проверки метки `meta mark 0xff`.

#### Этап 4: Веб-сервер Caddy и NaiveProxy (1 - 2 июля 2026 г. — v2.1.1)
* **Цель**: Запуск плагина NaiveProxy и решение проблем веб-сервера Caddy.
* **Что сделано**:
  * Написан генератор Caddyfile, развернут Caddy для автоматического выпуска SSL-сертификатов (Certbot / Let's Encrypt).
  * Настроен Decoy-сайт (сайт-заглушка) для маскировки под обычный HTTPS-трафик.
  * Внутренние порты Caddy/NaiveProxy (10443/10444) изолированы от внешней сети WAN через iptables для блокировки прямого сканирования.
  * **Решена проблема авторизации**: удалено избыточное bcrypt-хэширование, ломавшее basic_auth в форке Caddy, осуществлен переход на plaintext-пароли в Caddyfile.

#### Этап 5: Мультиплексирование (HAProxy Router) и TLS-транспорты (3 июля 2026 г. — v2.2.0-beta / v0.5 BETA)
* **Цель**: Обеспечение одновременной работы нескольких TLS-протоколов на одном порту 443.
* **Что сделано**:
  * Развернут **HAProxy** в качестве TLS-мультиплексора: он считывает SNI из пакета Client Hello и без расшифрования перенаправляет трафик на порты NaiveProxy, AnyTLS или TrustTunnel. Неизвестные домены уходят на decoy-заглушку.
  * Интегрирован плагин **AnyTLS** с генерацией безопасных ссылок.
  * Интегрирован плагин **TrustTunnel** (протокол обхода блокировок со схемой ссылок `tt://`).
  * Добавлен модуль выборочного роутинга **Cloudflare WARP** с суточным авто-обновлением списков блокировок (РФ-сервисы, GoogleAI, GEO-block).

#### Этап 6: Мониторинг, учет трафика и безопасность (3 июля 2026 г. — v2.2.0-beta / v0.5 BETA)
* **Цель**: Разработка полноценного биллинга, дашборда и защиты сервера.
* **Что сделано**:
  * Создан **Traffic Accounting Daemon** (`traffic_daemon.py`): фоновый демон парсит системный журнал `journalctl` и логи Caddy, сопоставляя IP и порты подключений с именами пользователей AnyTLS, Mieru и TrustTunnel для учета трафика.
  * Добавлены интерактивные TUI-пульты для систем защиты: **Fail2ban** (парсинг логов sing-box/sshd), **IPBan** (блокировка IP/CIDR/ASN) и **Honeypot** (ловушка сканирования портов с авто-баном).
  * Для предотвращения конфликтов TUI теперь автоматически отключает системный jail SSHD по умолчанию.

#### Этап 7: HTTPS-сервер подписок, Telemt (MTProxy) и qWDTT (4 июля 2026 г. — v2.3.0-beta)
* **Цель**: Автоматизация раздачи настроек клиентам и интеграция дополнительных утилит.
* **Что сделано**:
  * Запущен **HTTPS-сервер подписок на порту 9443**. Он генерирует ссылки формата NekoBox (`sn://`) и файлы конфигураций.
  * Интегрирована проверка лимитов трафика и TTL в реальном времени при запросе подписки.
  * Добавлен плагин **Telemt (MTProxy)** для Telegram с защитой от DPI и продвинутой статистикой по пользователям.
  * Интегрирован транспорт **qWDTT** (WireGuard over TURN) с изолированным парольным управлением (интеграция с Telegram-ботом в процессе разработки).
  * Разработана Опция 8 в главном меню TUI — интерактивный пульт VPS-диагностики (тесты GeoIP, скорость iPerf3 до РФ, тесты DPI через Censorcheck, глобальный замер скорости HTTP-загрузки).

#### Этап 8: Текущая стабилизация (5 июля 2026 г. — v2.3.1)
* **Цель**: Фиксы интеграции и развертывания системы «из коробки».
* **Что сделано**:
  * Оркестратор теперь немедленно перезапускает сервер подписок при блокировке или удалении пользователя для отзыва его сессии.
  * Изменен формат ссылок NaiveProxy на `naive+https://`.
  * Автоматизирована миграция старых параметров jail Fail2ban при обновлении конфигурационных файлов.
  * Исправлены ошибки первого запуска на чистой ОС (проверка наличия каталогов во время установки).
</details>

---

## Разработка

```bash
git clone https://github.com/gr33nimax/HYDRA-ULTIMATE.git
cd HYDRA-ULTIMATE
python -m pytest tests/ -v   # 230+ тестов за 7 секунд
sudo python3 main.py         # TUI (только Linux)
```

---

## Лицензия

MIT License — см. [LICENSE](LICENSE)
