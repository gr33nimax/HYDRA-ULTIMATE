# ИНТЕГРАЦИЯ НОВЫХ МОДУЛЕЙ В _core.py
# =====================================
# Два модуля: port_hopping.py и tg_bot.py
# Всего ~8 изменений в _core.py, ни одного удаления существующей логики на первом шаге.

# ═══════════════════════════════════════════════════════════════════════════
# ШАГ 1. Добавить импорты (в блок импортов модулей, примерно строки 62–140)
# ═══════════════════════════════════════════════════════════════════════════

# Добавить после последнего from vless_installer.modules.* :

from vless_installer.modules.port_hopping import (
    do_port_hopping_menu,
    ph_status,
)
from vless_installer.modules.tg_bot import (
    do_manage_telegram,       # заменяет одноимённую функцию в _core.py
    do_tg_bot_menu,           # новая
    tg_load  as _tg_load,     # псевдоним — весь код в _core.py через _tg_load() продолжит работать
    tg_save  as _tg_save,
    tg_send,
    tg_notify_event as _tg_notify_event,
    TG_CONFIG_FILE,
)


# ═══════════════════════════════════════════════════════════════════════════
# ШАГ 2. Удалить дублирующиеся определения из _core.py
# ═══════════════════════════════════════════════════════════════════════════
# В _core.py TG_CONFIG_FILE определяется 3 раза, а _tg_load/_tg_save/tg_send/
# _tg_notify_event/do_manage_telegram определены один раз (~строки 21362–21545).
# После добавления импортов выше — удалить эти определения из _core.py.
# ВСЕ вызовы _tg_load(), _tg_save(), tg_send(), _tg_notify_event() в _core.py
# продолжат работать без изменений — имена совпадают.
#
# Также удалить строки вида:
#   TG_CONFIG_FILE = Path('/var/lib/xray-installer/telegram.json')  # дубли на ~21865, ~22920
# Они теперь импортируются из tg_bot.py.


# ═══════════════════════════════════════════════════════════════════════════
# ШАГ 3. Port Hopping — добавить пункт меню
# ═══════════════════════════════════════════════════════════════════════════
# Найти функцию _menu_install_system() или главное меню (~строка 28304).
# Добавить в подходящий раздел (например "Безопасность" или "Сеть"):

# В _box_item добавить:
_box_item("PH", f"⚡ Port Hopping  {DIM}(диапазон портов против блокировки){NC}")

# В обработчик выбора добавить:
elif ch in ("ph", "PH"):
    do_port_hopping_menu()


# ═══════════════════════════════════════════════════════════════════════════
# ШАГ 4. Telegram Bot — добавить пункт меню
# ═══════════════════════════════════════════════════════════════════════════
# Найти раздел меню где сейчас находится пункт Telegram-уведомлений.
# Рядом добавить:

_box_item("TB", f"🤖 Telegram Bot  {DIM}(раздача конфигов пользователям){NC}")

# В обработчик:
elif ch in ("tb", "TB"):
    do_tg_bot_menu()


# ═══════════════════════════════════════════════════════════════════════════
# ШАГ 5 (опционально). Показывать статус port hopping в do_quick_status()
# ═══════════════════════════════════════════════════════════════════════════
# Найти do_quick_status() (~строка 26604) и добавить:

ph = ph_status()
if ph.get("enabled"):
    _box_row(f"  Port Hopping: {GREEN}ВКЛЮЧЁН{NC}  "
             f"{ph['range_start']}–{ph['range_end']} → {ph['real_port']}")


# ═══════════════════════════════════════════════════════════════════════════
# ПРИМЕЧАНИЯ
# ═══════════════════════════════════════════════════════════════════════════
# - do_manage_telegram() теперь импортируется из tg_bot.py.
#   Если в _core.py есть своя одноимённая функция — удалить её,
#   импортированная заменяет полностью (интерфейс идентичен).
#
# - _failover_install() в _core.py использует inline curl для TG (~строка 19963).
#   Можно оставить как есть — он не зависит от _tg_load.
#   Опционально заменить на: tg_send(msg, tg_token, tg_chat)
#
# - Бот работает как отдельный systemd-сервис xray-tg-bot.
#   Не влияет на xray.service, nginx.service, awg — полностью независим.
#
# - Port hopping не трогает config.json Xray.
#   Единственный side effect — правила iptables NAT PREROUTING.
#   При отключении удаляет только свои правила (по метке xray-port-hopping).
